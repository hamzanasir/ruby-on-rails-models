%p#notice= notice

%article
  %h2= @book.title
  %h3= @book.authors.length == 1 ? 'Author:' : 'Authors:'
  %ul
    - @book.authors.each do |author|
      %li= author.full_name

= link_to 'Edit', edit_book_path(@book)
\|
= link_to 'Back', books_path
